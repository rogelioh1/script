# windows-qos-script

This python code creates powershell scripts based on user input that throttles the outbound traffic of specific applications and runs them. You can also remove previously applied policies. This can be useful if you're regularly enabling and disabling bandwidth limits on an application.
